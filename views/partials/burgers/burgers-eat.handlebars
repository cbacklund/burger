<li>
	{{burger_name}}

	<button class="change-eat" data-id="{{id}}" data-neweat="{{devoured}}">
		{{#if devoured}}Crush it!!{{else}}Eat it Again!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Delete Forever!</button>
</li>